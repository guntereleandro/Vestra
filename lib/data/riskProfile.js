export const RISK_PROFILE_KEY = "vestra:riskProfile:v1";
export const EMPTY_RISK_PROFILE = Object.freeze({ experienceLevel: "", investmentHorizonYears: null, liquidityNeed: "", incomeStability: "", lossTolerancePercent: null, emergencyReserveStatus: "", primaryObjective: "", answers: Object.freeze({}), calculatedProfile: "", calculatedAt: "", updatedAt: "" });
export const RISK_OPTIONS = Object.freeze({ experienceLevel: ["beginner", "intermediate", "advanced"], liquidityNeed: ["high", "medium", "low"], incomeStability: ["low", "medium", "high"], emergencyReserveStatus: ["none", "partial", "complete"], primaryObjective: ["preserve_capital", "income", "balanced_growth", "long_term_growth"], calculatedProfile: ["conservative", "moderate", "aggressive"] });

export function normalizeRiskProfile(value = {}) { const source = value && typeof value === "object" && !Array.isArray(value) ? value : {}; return { experienceLevel: allowed("experienceLevel", source.experienceLevel), investmentHorizonYears: validRange(source.investmentHorizonYears, 0, 100), liquidityNeed: allowed("liquidityNeed", source.liquidityNeed), incomeStability: allowed("incomeStability", source.incomeStability), lossTolerancePercent: validRange(source.lossTolerancePercent, 0, 100), emergencyReserveStatus: allowed("emergencyReserveStatus", source.emergencyReserveStatus), primaryObjective: allowed("primaryObjective", source.primaryObjective), answers: source.answers && typeof source.answers === "object" && !Array.isArray(source.answers) ? { ...source.answers } : {}, calculatedProfile: allowed("calculatedProfile", source.calculatedProfile), calculatedAt: timestamp(source.calculatedAt), updatedAt: timestamp(source.updatedAt) }; }
export function validateRiskProfile(value = {}) { const normalized = normalizeRiskProfile(value), errors = []; for (const field of ["experienceLevel", "liquidityNeed", "incomeStability", "emergencyReserveStatus", "primaryObjective"]) if (value[field] && !RISK_OPTIONS[field].includes(value[field])) errors.push(`${field}: valor inválido.`); if (value.investmentHorizonYears !== null && value.investmentHorizonYears !== "" && normalized.investmentHorizonYears === null) errors.push("investmentHorizonYears: valor inválido."); if (value.lossTolerancePercent !== null && value.lossTolerancePercent !== "" && normalized.lossTolerancePercent === null) errors.push("lossTolerancePercent: valor inválido."); return { valid: errors.length === 0, errors, value: normalized }; }
export function hasRiskProfile(value) { return Boolean(normalizeRiskProfile(value).calculatedProfile); }
export function readRiskProfile() { if (typeof localStorage === "undefined") return { ...EMPTY_RISK_PROFILE }; try { return normalizeRiskProfile(JSON.parse(localStorage.getItem(RISK_PROFILE_KEY) || "{}")); } catch { return { ...EMPTY_RISK_PROFILE }; } }
export function writeRiskProfile(value) { const result = validateRiskProfile(value); if (!result.valid) throw new Error("INVALID_RISK_PROFILE"); const saved = { ...result.value, updatedAt: new Date().toISOString() }; localStorage.setItem(RISK_PROFILE_KEY, JSON.stringify(saved)); return saved; }
function allowed(field, value) { return RISK_OPTIONS[field].includes(value) ? value : ""; }
function validRange(value, min, max) { if (value === "" || value === null || value === undefined || !Number.isFinite(Number(value)) || Number(value) < min || Number(value) > max) return null; return Number(value); }
function timestamp(value) { return typeof value === "string" && Number.isFinite(Date.parse(value)) ? new Date(value).toISOString() : ""; }

import Link from "next/link";
import { getArticleHref, getCategoryLabel } from "@/lib/knowledge/knowledgeService";
export default function KnowledgeArticle({ article }) { return <article className="card rounded-2xl p-5 transition hover:border-[#d9b86c]/25"><div className="flex items-start justify-between gap-3"><div><p className="eyebrow">{getCategoryLabel(article.category)}</p><h2 className="font-display mt-2 text-2xl">{article.title}</h2></div><span className="shrink-0 text-[10px] text-[#626762]">v{article.version}</span></div><p className="mt-3 text-sm leading-6 text-[#858b86]">{article.description}</p><div className="mt-4 flex flex-wrap gap-2">{article.tags.map((tag) => <span key={tag} className="rounded-full border border-white/[.07] px-2.5 py-1 text-[10px] text-[#747a75]">{tag}</span>)}</div><Link href={getArticleHref(article)} className="mt-5 inline-flex rounded-lg text-xs font-semibold text-[#d9b86c] outline-none focus-visible:ring-2 focus-visible:ring-[#d9b86c]/60">Ler artigo completo →</Link></article>; }

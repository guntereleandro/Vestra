export default function RiskQuestion({ label, description, children }) { return <label className="block"><span className="text-xs font-semibold text-[#c9ccc8]">{label}</span>{description && <span className="mt-1 block text-[11px] text-[#6f756f]">{description}</span>}<span className="mt-2 block">{children}</span></label>; }

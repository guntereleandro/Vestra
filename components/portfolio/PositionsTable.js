import { BriefcaseBusiness, Edit3 } from "lucide-react";
import { currency, percent, quantity } from "@/lib/engine/totals";

export default function PositionsTable({ positions, emptyMessage = "Nenhuma posição em carteira", onEditQuote }) {
  if (!positions.length) return <div className="px-5 py-16 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl border border-[#d9b86c]/20 bg-[#d9b86c]/5 text-[#d9b86c]"><BriefcaseBusiness size={22} /></span><h3 className="font-display mt-5 text-xl">{emptyMessage}</h3><p className="mx-auto mt-2 max-w-md text-sm text-[#777d78]">Registre uma compra em Operações para formar sua carteira.</p></div>;
  const heads = ["Ativo", "Tipo", "Quantidade", "Preço médio", "Cotação atual", "Valor atual", "Resultado", "Proventos", ...(onEditQuote ? ["Ações"] : [])];
  return <div className="p-3 sm:overflow-x-auto sm:p-0"><table className="asset-table w-full border-collapse text-left"><thead><tr className="text-[9px] uppercase tracking-[.14em] text-[#626762]">{heads.map((head) => <th key={head} className="border-b border-white/[.06] px-5 py-4">{head}</th>)}</tr></thead><tbody>{positions.map((position) => { const gain = position.profit >= 0; return <tr key={position.ticker} className="hover:bg-white/[.018]"><td className="border-b border-white/[.05] px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-lg border border-[#d9b86c]/15 bg-[#d9b86c]/5 text-[10px] font-bold text-[#d9b86c]">{position.ticker.slice(0, 2)}</span><div><p className="text-sm font-bold">{position.ticker}</p><p className="text-[10px] text-[#777d78]">{position.name}</p></div></div></td><td data-label="Tipo" className="asset-cell">{position.type}</td><td data-label="Quantidade" className="asset-cell">{quantity.format(position.quantity)}</td><td data-label="Preço médio" className="asset-cell">{currency.format(position.averagePrice)}</td><td data-label="Cotação atual" className="asset-cell"><p>{currency.format(position.currentPrice)}</p>{!position.hasQuote && <p className="mt-1 text-[9px] font-bold text-amber-400">Cotação não informada</p>}</td><td data-label="Valor atual" className="asset-cell font-bold text-white">{currency.format(position.currentValue)}</td><td data-label="Resultado" className="asset-cell"><p className={`font-bold ${gain ? "text-emerald-400" : "text-rose-400"}`}>{gain ? "+" : ""}{currency.format(position.profit)}</p><p className={`text-[10px] ${gain ? "text-emerald-400/70" : "text-rose-400/70"}`}>{gain ? "+" : ""}{percent.format(position.profitability)}%</p></td><td data-label="Proventos" className="asset-cell">{currency.format(position.dividends)}</td>{onEditQuote && <td className="border-b border-white/[.05] px-5 py-4"><button onClick={() => onEditQuote(position)} aria-label={`Editar cotação ${position.ticker}`} className="icon-button"><Edit3 size={15} /></button></td>}</tr>; })}</tbody></table></div>;
}

import { currency } from "@/lib/engine/totals";
export default function ContributionCard({ growth, contributions }) { const origins = [["Aportes", growth.contributions, growth.originPercentages.contributions], ["Valorização", growth.appreciation, growth.originPercentages.appreciation], ["Proventos", growth.income, growth.originPercentages.income]]; return <article className="card rounded-2xl p-5"><p className="eyebrow">Origem do crescimento</p><div className="mt-4 space-y-3">{origins.map(([label, value, percent]) => <div key={label} className="flex items-center justify-between gap-4"><div><p className="text-xs text-[#9ba09b]">{label}</p><p className="mt-1 text-sm font-semibold">{currency.format(value)}</p></div><p className="text-lg font-semibold text-[#f1e4bd]">{format(percent)}%</p></div>)}</div><div className="mt-5 grid gap-4 border-t border-white/[.06] pt-4 sm:grid-cols-2"><Ranking title="Maiores contribuições" items={contributions.best} /><Ranking title="Menores contribuições" items={contributions.worst} /></div></article>; }
function Ranking({ title, items }) { return <div><h4 className="text-xs font-semibold text-[#c8cbc7]">{title}</h4>{items.length ? <ul className="mt-2 space-y-1.5">{items.slice(0, 3).map((item) => <li key={item.ticker} className="flex justify-between gap-3 text-xs"><span className="text-[#858b86]">{item.ticker}</span><span className={item.value >= 0 ? "text-emerald-300" : "text-rose-300"}>{item.value >= 0 ? "+" : ""}{currency.format(item.value)}</span></li>)}</ul> : <p className="mt-2 text-xs text-[#666c67]">Sem dados disponíveis.</p>}</div>; }
function format(value) { return Number(value.toFixed(1)).toLocaleString("pt-BR"); }

import { currency } from "@/lib/engine/totals";
export default function WealthGrowthCard({ growth, score }) { return <article className="card rounded-2xl p-5"><p className="eyebrow">Evolução</p><p className={`mt-3 text-3xl font-semibold ${growth.growth >= 0 ? "text-emerald-300" : "text-rose-300"}`}>{growth.growth >= 0 ? "+" : ""}{currency.format(growth.growth)}</p><p className="mt-2 text-xs text-[#858b86]">Rentabilidade estimada: {format(growth.profitabilityPercent)}%</p><div className="mt-4 border-t border-white/[.06] pt-3"><p className="text-xs text-[#9ba09b]">Consistência patrimonial</p><p className="mt-1 text-xl font-semibold text-[#f1e4bd]">{score.value}/100</p><p className="mt-1 text-[11px] text-[#6f756f]">Confiança {format(score.confidence * 100)}%</p></div></article>; }
function format(value) { return Number(value.toFixed(1)).toLocaleString("pt-BR"); }
